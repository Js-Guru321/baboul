import { BaboulCategoryTreePipe } from './baboul-category-tree.pipe';

describe('BaboulCategoryTreePipe', () => {
  it('create an instance', () => {
    const pipe = new BaboulCategoryTreePipe();
    expect(pipe).toBeTruthy();
  });
});
