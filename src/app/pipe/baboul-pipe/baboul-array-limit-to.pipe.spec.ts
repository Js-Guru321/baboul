import { BaboulArrayLimitToPipe } from './baboul-array-limit-to.pipe';

describe('BaboulArrayLimitToPipe', () => {
  it('create an instance', () => {
    const pipe = new BaboulArrayLimitToPipe();
    expect(pipe).toBeTruthy();
  });
});
