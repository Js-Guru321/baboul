import { StoreOpenedPipe } from './store-opened.pipe';

describe('StoreOpenedPipe', () => {
  it('create an instance', () => {
    const pipe = new StoreOpenedPipe();
    expect(pipe).toBeTruthy();
  });
});
