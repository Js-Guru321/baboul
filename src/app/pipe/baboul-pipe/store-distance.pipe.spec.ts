import { StoreDistancePipe } from './store-distance.pipe';

describe('StoreDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new StoreDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
