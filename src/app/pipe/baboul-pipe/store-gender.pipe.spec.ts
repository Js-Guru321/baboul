import { StoreGenderPipe } from './store-gender.pipe';

describe('StoreGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new StoreGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
