import { BaboulArrayFilterPipePipe } from './baboul-array-filter-pipe.pipe';

describe('BaboulArrayFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BaboulArrayFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
