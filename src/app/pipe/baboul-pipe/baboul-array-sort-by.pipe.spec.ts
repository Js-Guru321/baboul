import { BaboulArraySortByPipe } from './baboul-array-sort-by.pipe';

describe('BaboulArraySortByPipe', () => {
  it('create an instance', () => {
    const pipe = new BaboulArraySortByPipe();
    expect(pipe).toBeTruthy();
  });
});
